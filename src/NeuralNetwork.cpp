#include "NeuralNetwork.h"
#include <Eigen/Dense>
#include <vector>

using namespace Eigen;
using namespace std;

namespace NeuralNetwork {
    Network::Network(int inputs, int hiddenShape[2], int outputs, const double learningRate) : learningRate(learningRate) {
        // :)
    }

}